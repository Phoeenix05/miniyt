import { component$ } from '@builder.io/qwik'

export type ChannelBannerProps = {
    channelId: string
}

export const ChannelBanner = component$<ChannelBannerProps>((props) => {
    return <div></div>
})
